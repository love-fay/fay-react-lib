import React from 'react';
import Layout from '../../layout/1-1';
import Division from 'court-division';

export default () => {
    return (
        <Layout minWidth={0}>
            <Division offsetTop={50}/>
        </Layout>
    )
}