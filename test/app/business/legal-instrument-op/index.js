import React from 'react';
import Layout from '../../layout/1-1';
import LegalInstrumentOp from 'court-legal-instrument-op';

export default () => {
    return (
        <Layout minWidth={0}>
            <LegalInstrumentOp offsetTop={50}/>
        </Layout>
    )
};